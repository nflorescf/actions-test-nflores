name: Scout Analysis
on: [push]
jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - uses: coinfabrik/scout-actions@test.v1.0.0
        with:
          target: './target/test.toml'
